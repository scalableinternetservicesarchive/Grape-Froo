
<p>
  <strong>Name:</strong>
  <%= @template.name %>
</p>

<p>
  <%= image_tag(@template.image.url, :id => "template") %>
</p>

<p>
  submitted <%= time_ago_in_words(@template.created_at) %> ago by <em><%= @template.user %></em>
</p>

<%= link_to 'Edit', edit_template_path(@template) %> |
<%= link_to 'Back', templates_path %>
