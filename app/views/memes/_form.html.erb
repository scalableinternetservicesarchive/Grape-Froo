<%= form_for(meme) do |f| %>
  <% if meme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meme.errors.count, "error") %> prohibited this meme from being saved:</h2>

      <ul>
      <% meme.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :top_caption %>
    <%= f.text_field :top_caption %>
  </div>

  <div class="field">
    <%= f.label :bottom_caption %>
    <%= f.text_field :bottom_caption %>
  </div>

  <div class="field">
    <%= f.label :template_id %>
    <%= f.text_field :template_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
